# TrackVolt — Cross-Agent Sync Brief
> Generated by Claude (Cowork) on 2026-03-06
> Another AI agent (Claude Cowork) is actively working on this same project.
> Read this file COMPLETELY to understand current state before making any changes.

---

## PRIMARY GOAL (from the user, Aron)

**Goal #1: Make the local codebase at `C:\ClaudeWork\Imu\TrackVolt-App\` match exactly what is live at https://trackvolt.app — so it builds, deploys, and works identically.**

**Goal #2: Prepare for iOS deployment.** Once local matches live, the next step is wrapping this PWA for the App Store (likely via Capacitor or similar). Everything needs to be ready.

**Goal #3: PORTABILITY.** Everything must live inside `C:\ClaudeWork\Imu\`. If Aron moves to a new laptop, copying this one folder should give him the complete project — source code, assets, logos, docs, config, everything. No dependencies on files outside this folder. No absolute paths baked in. No references to locations outside `C:\ClaudeWork\Imu\`.

---

## Project Overview
- **App:** TrackVolt — CrossFit/fitness PWA (supports all genders, ages, cycle tracking, nutrition, timers, achievements)
- **Stack:** React 19 + TypeScript + Vite 7 + Tailwind CSS v4 + vite-plugin-pwa
- **State:** Zustand 5 | **DB:** Dexie 4 (IndexedDB) | **Deploy:** Vercel (project: "crosstrack")
- **Live site:** https://trackvolt.app
- **Source root:** `C:\ClaudeWork\Imu\TrackVolt-App\`
- **Parent folder (EVERYTHING must be inside here):** `C:\ClaudeWork\Imu\`

## Safety Rules (MUST FOLLOW — NON-NEGOTIABLE)
- **No deploy** without user saying "CONFIRM DEPLOY"
- **No destructive file ops** (delete/overwrite) without "CONFIRM DESTRUCTIVE"
- **No secrets/env changes** without "CONFIRM SECRETS"
- **No feature changes** without "CONFIRM BUILD"
- **Live production (trackvolt.app) is the source of truth**, not any local branch or old backup

## What Claude (Cowork) Has Done So Far (Sessions 0–10)

### Session History (brief)
- **Sessions 0-3:** Initial project setup, component scaffolding, i18n
- **Sessions 4-6:** Extracted all 142 production source files from live site localStorage, decoded base64, wrote to Windows, fixed 6 categories of TypeScript build errors
- **Sessions 7-9:** WorkoutLogger.tsx deep form parity (CrossFit form enhancements — this file is INTENTIONALLY different from production)
- **Session 10 (today):** Full local-vs-live audit, icon sync, types alignment, build verification

### Completed Today
1. **Full audit** of local vs live production — identified all file gaps
2. **Batch 1 (Icons):** Copied 4 branded icons to `public/`:
   - `icon-192.png`, `icon-512.png`, `icon-512-maskable.png`, `apple-touch-icon.png`
   - Source: `TrackVolt Logo/06-App-Icons/`
3. **Batch 5 (types/index.ts alignment):** Removed 6 dead interfaces not in production:
   - `UserSettings`, `CycleEntry`, `BodyMeasurement`, `HeartRateLog`, `PhotoLog`, `Achievement`
   - Made `MovementPR.createdAt` and `WeeklyPlan.createdAt` required (was optional)
4. **Batch 6 (Build verification):** `tsc --noEmit` and `vite build` both pass clean

### Waiting on User Action
- **Manual binary copy** (Cowork tools can't write binary files):
  ```cmd
  copy "C:\ClaudeWork\Imu\TrackVolt-App\TrackVolt Logo\07-Email-Extras\OG-Image-1200x630.png" "C:\ClaudeWork\Imu\TrackVolt-App\public\og-image.png"
  copy "C:\ClaudeWork\Imu\TrackVolt-App\TrackVolt Logo\06-App-Icons\favicon.ico" "C:\ClaudeWork\Imu\TrackVolt-App\public\favicon.ico"
  ```

### Waiting on "CONFIRM DESTRUCTIVE"
These files need to be deleted (stray duplicates + dead code):

**Root-level strays (duplicates of files already in src/):**
- `BarcodeScanner.tsx`, `CycleCalendar.tsx`, `CycleLogger.tsx`, `MealLogger.tsx`
- `useCycleTracking.ts`, `useWorkoutForm.ts`, `zh-CN.ts`

**Dead code in src/ (not imported by any production file):**
- `src/components/PlaceholderPage.tsx`
- `src/components/Toast.tsx`
- `src/components/InstallBanner.tsx`
- `src/data/foodLibrary.ts`
- `src/data/movements.ts`

**Leftovers:**
- `public/decode-favicon.mjs` (helper script, should be removed)
- `src/i18n/zhCN.ts.bak` (backup file)

## Files You MUST NOT Overwrite
- **`src/components/log/WorkoutLogger.tsx`** — The local version is intentionally DIFFERENT from production. It has the user's custom enhanced CrossFit form parity work (Sessions 7-9). DO NOT replace with production version.

## Key Files to Know
| File | Purpose |
|------|---------|
| `CHANGELOG_TRACKVOLT.md` | Full project memory (Sessions 0-9) |
| `AGENTS.md` | Agent coordination notes |
| `TRACKVOLT_MASTER_DOC.md` | Architecture & feature reference |
| `CODEX_SYNC.md` | THIS FILE — cross-agent sync brief |
| `src/types/index.ts` | Type definitions — just aligned with production today |
| `src/types/eventTypes.ts` | CrossFit event types |
| `src/stores/useStore.ts` | Zustand store (single source of app state) |
| `src/db/database.ts` | Dexie database schema |
| `vite.config.ts` | PWA manifest, icons, screenshots config |
| `TrackVolt Logo/` | All branded assets (icons, social, OG images) |

## Folder Structure (portability)
```
C:\ClaudeWork\Imu\
└── TrackVolt-App\           ← EVERYTHING lives here
    ├── src\                  ← Source code
    ├── public\               ← Static assets (icons, screenshots, manifest)
    ├── TrackVolt Logo\       ← Brand assets (icons, social media, OG images)
    ├── node_modules\         ← Dependencies (regenerated by npm install)
    ├── dist\                 ← Build output (regenerated by vite build)
    ├── package.json          ← Dependencies & scripts
    ├── vite.config.ts        ← Build config + PWA manifest
    ├── tsconfig.json         ← TypeScript config
    ├── tailwind.config.ts    ← Tailwind config
    ├── .env                  ← Environment variables (Firebase, Gemini keys)
    ├── CHANGELOG_TRACKVOLT.md
    ├── TRACKVOLT_MASTER_DOC.md
    ├── AGENTS.md
    └── CODEX_SYNC.md         ← This file
```

**PORTABILITY CHECK:** If ANY file references an absolute path outside `C:\ClaudeWork\Imu\`, that's a bug. Flag it. All paths should be relative within the project.

## iOS Deployment Readiness Checklist
When local matches live, these are needed for iOS:
- [ ] Capacitor (or similar) integration for native iOS wrapper
- [ ] App icons in all required iOS sizes (some already in `TrackVolt Logo/06-App-Icons/`)
- [ ] Splash screens / launch images
- [ ] App Store metadata (description, keywords, screenshots)
- [ ] Apple Developer account setup (user handles this)
- [ ] Push notification setup (if needed)
- [ ] Capacitor config pointing to the Vite build output

## How to Coordinate With Claude (Cowork)
1. If you make changes, note them in `CHANGELOG_TRACKVOLT.md`
2. If you find issues or have suggestions, write them to `CODEX_FINDINGS.md` so Claude can read them
3. Before modifying any file, check if it was recently changed
4. The codebase itself is the shared workspace between us
5. Follow the safety rules — they protect Aron's live app

## Current Build Status
- **TypeScript:** CLEAN (0 errors)
- **Vite build:** CLEAN (68 precache entries, 2671 KiB)
- **All 99 production source files synced to local**

## User Note
Aron has ADHD (PI/ADD). Keep summaries clear, concise, and action-oriented. Don't bury important info in walls of text.
